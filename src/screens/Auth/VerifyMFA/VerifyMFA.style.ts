import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    width: '100%',
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    maxWidth: 300,
    marginLeft: 'auto',
    marginRight: 'auto',
  },
  input: {
    marginVertical: 10,
    height: 50,
    fontSize: 13,
  },
  button: {
    marginVertical: 20, 
    marginBottom: 15,
    height: 50,
    justifyContent: 'center',
    alignItems: 'center',
  },
})
