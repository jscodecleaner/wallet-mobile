
import React, { useState } from 'react';
import { TextInput, Button, Text, HelperText, withTheme } from 'react-native-paper';

const ChangePhone = ({ theme, navigation }) => {
    return (
        <Text>changePhone</Text>
    );
};

export default withTheme(ChangePhone);
